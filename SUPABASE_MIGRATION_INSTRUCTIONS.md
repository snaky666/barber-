# 🔄 تعليمات ربط الموقع بالكامل مع Supabase

## ✅ ما تم إنجازه

تم تحديث جميع ملفات الكود لاستخدام Supabase بالكامل بدلاً من localStorage و JSON files:

### البيانات المرتبطة بـ Supabase:
- ✅ الحجوزات (Bookings)
- ✅ الأيام الملغاة (Cancelled Days)
- ✅ الإعلانات (Announcements)
- ✅ السجل (Journal)
- ✅ الدخل (Income)
- ✅ الديون (Debt)
- ✅ **أيام العمل (Workdays)** - تم إضافتها الآن!

### ما يبقى محلياً (للأمان):
- بيانات تسجيل الدخول (Credentials) - تبقى في localStorage

---

## 📋 خطوات التنفيذ (مهمة جداً!)

### الخطوة 1: تطبيق السكريبت SQL المحدث

1. افتح مشروعك في Supabase: https://app.supabase.com
2. انتقل إلى **SQL Editor**
3. افتح الملف `SQL_FOR_SUPABASE.sql` من هذا المشروع
4. انسخ **جميع** محتويات الملف
5. الصق المحتوى في SQL Editor
6. اضغط على **Run** (تشغيل)
7. يجب أن ترى رسالة: `Success. No rows returned` ✅

### الخطوة 2: التحقق من الجداول

تأكد من وجود جميع الجداول التالية في قاعدة البيانات:
- `bookings`
- `cancelled_days`
- `announcements`
- `journal`
- `income`
- `debt`
- `workdays` ← **الجدول الجديد!**

### الخطوة 3: إعادة تشغيل الموقع

1. قم بتحديث الصفحة في المتصفح (F5 أو Ctrl+R)
2. سيتم تلقائياً:
   - تحميل البيانات من Supabase
   - ترحيل أيام العمل من localStorage إلى Supabase
   - حذف البيانات المحلية القديمة

---

## 🎯 الميزات الجديدة

### 1. أيام العمل مخزنة في السحابة
- لن تفقد إعدادات أيام العمل عند مسح ذاكرة المتصفح
- يمكن الوصول إليها من أي متصفح أو جهاز

### 2. لا حاجة لـ server.py و data.json
- جميع البيانات الآن في Supabase
- الموقع يعمل بشكل كامل كـ Static Site
- يمكن نشره على أي استضافة ثابتة (Static Hosting)

### 3. المزامنة التلقائية
- جميع التحديثات تُحفظ تلقائياً في Supabase
- التحديثات الفورية (Real-time) للحجوزات والإعلانات

---

## ⚠️ ملاحظات مهمة

1. **يجب تطبيق السكريبت SQL المحدث** - يحتوي على جدول workdays الجديد
2. **الترحيل التلقائي** - سيتم ترحيل أيام العمل من localStorage مرة واحدة فقط
3. **بيانات الدخول آمنة** - تبقى في localStorage ولا تُرسل إلى Supabase

---

## 🔍 التحقق من النجاح

افتح Console في المتصفح (F12) وتأكد من رؤية:
```
📦 تم تحميل عميل Supabase
📥 جاري تحميل البيانات من Supabase...
✅ تم تحميل البيانات بنجاح
✅ طبقة البيانات جاهزة وتم تحميل البيانات من Supabase
🔄 ترحيل أيام العمل من localStorage إلى Supabase...
💾 تم حفظ أيام العمل في Supabase
✅ تم ترحيل أيام العمل إلى Supabase
```

---

## 📞 في حال وجود مشاكل

1. افتح Console (F12) وابحث عن رسائل الخطأ باللون الأحمر
2. تأكد من:
   - تطبيق السكريبت SQL بشكل صحيح
   - إعدادات Supabase في `js/supabase-config.js` صحيحة
   - اتصالك بالإنترنت مستقر

---

## ✨ الخلاصة

الموقع الآن مرتبط بالكامل مع Supabase! جميع البيانات (بما فيها أيام العمل) تُحفظ في السحابة وتتزامن تلقائياً. لا حاجة لملفات JSON أو localStorage (باستثناء بيانات الدخول للأمان).
